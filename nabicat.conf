limit_req_zone $binary_remote_addr zone=ratelimit:10m rate=5r/s;

server {
	listen 80;
	server_name nabicat.site;
	client_max_body_size 1G;
	if ($http_user_agent ~* (scrapy|bot|spider|crawl|scanner) ) {
    	return 403;
	}
	if ($http_user_agent = "") {
    	return 403;
	}

	return 301 https://$host$request_uri; # Redirect to HTTPS

	# location / {
	# 	proxy_pass http://127.0.0.1:5000;
	# 	proxy_set_header Host $host;
	# 	proxy_set_header X-Real-IP $remote_addr;
	# 	proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
	# 	limit_req zone=ratelimit burst=10 nodelay;
	# }
}

server {
	listen 443 ssl;
	server_name nabicat.site;
	ssl_certificate /etc/letsencrypt/live/nabicat.site/fullchain.pem;
	ssl_certificate_key /etc/letsencrypt/live/nabicat.site/privkey.pem;
	client_max_body_size 1G;
	if ($http_user_agent ~* (scrapy|bot|spider|crawl|scanner) ) {
    	return 403;
	}
	if ($http_user_agent = "") {
    	return 403;
	}
	location / {
		proxy_pass http://127.0.0.1:5000;
		proxy_set_header Host $host;
		proxy_set_header X-Real-IP $remote_addr;
		proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
		proxy_connect_timeout 300;
		proxy_send_timeout 300;
		proxy_read_timeout 300;
		limit_req zone=ratelimit burst=10 nodelay;
	}
}

# Redirect old domain to new domain
server {
	listen 80;
	server_name lazywombat.site;
	return 301 https://nabicat.site$request_uri;
}

server {
	listen 443 ssl;
	server_name lazywombat.site;
	ssl_certificate /etc/letsencrypt/live/lazywombat.site/fullchain.pem;
	ssl_certificate_key /etc/letsencrypt/live/lazywombat.site/privkey.pem;
	return 301 https://nabicat.site$request_uri;
}
